import { AnimatedLogo } from "./logo";

const Loading = () => {
  return (
    <div className="loading-container">
      <AnimatedLogo width={200} theme="dark" />
    </div>
  );
};

export default Loading;
